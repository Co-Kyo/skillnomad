# skillnomad-types

**内部实现包**：skillnomad 的类型系统与 `task` / `seq` / `parallel` / `mapNode` 等构建函数。

对外请使用 [`skillnomad`](https://www.npmjs.com/package/skillnomad)（主包全量转口，`import { ... } from 'skillnomad'`）——请勿直接安装或 import 本包。

# 核心契约：步骤之间是线性链

> **写给谁**：已经会写步骤、想知道框架到底保证什么、不保证什么的人。第一次接触先看[快速上手](quickstart)。

这一页是 skillnomad 对使用者的**公开承诺**，不读源码也应当知道。

skillnomad 由 **TypeScript** 编写，类型定义随包发布——本节写下的保证是**类型层面的**，不是文档里的约定。

**顶层 step 是不可并行的执行单位。** 步骤之间的关系是一条**线性链**，不是 DAG：

| 层次 | 能否并行 | 怎么表达 |
| :--- | :--- | :--- |
| 顶层 step 之间 | **不可并行** | 线性链；只声明一个方向即可 |
| step 内部 | 可并行 | `.parallel()` / `.map()`，由 `gate` 收敛、`maxConcurrency` 控制并发度 |

## 三条使用规则

1. **不要把可并行的动作拆成多个顶层步骤。** 需要并行就在一步内部用 `.parallel()` / `.map()` 表达。
2. **`.dependsOn()` 只传一个前驱（单值），与 `.next()` 互为反函数。** 另一个由框架推导；同时声明属冗余，也埋下不一致的风险。
3. **`next` 由框架推导，无需手写。** 框架从链序算出每一步的下一跳（末步为终止标记 `done`）；显式声明可覆盖推导值。

## 类型级保证：写错在编译期就不可能

`dependsOn` 单值不是靠构建期校验兜底，而是**类型层面**就保证了「写两个前驱不可能」——
违反契约（多依赖 / 成环 / 断链 / 悬空引用）在构建期报错，**不会静默线性化**。

链校验在 `skillnomad build` 与 `skillnomad validate` 两处都会执行；
所有推导函数在链不成立时一律**返回空而不猜测**，交由校验报错说明原因。

## 阶段（phases）：只声明意图，不声明区间

`phases` 声明每个阶段**包含哪些步骤**——这是事实。「第几步到第几步」是顺序的副产物，由框架从链序推导，**不要求也不应该手写**：

```ts
// 推荐：只声明阶段包含哪些步骤
phases: [
  { name: '初始化', stepIds: ['initialize'], description: '...' },
  { name: '前处理', stepIds: ['scan', 'capability-graph', 'evaluate-pool'], description: '...' },
]
// → 框架推导出 `(00)` / `(04-06)`，并生成两行流程总览
```

阶段是派生值的**唯一输入**，因此必须自洽。构建期断言四条：

| 约束 | 不满足的后果 |
| :--- | :--- |
| 引用的步骤必须存在 | 区间无意义 → 报错 |
| 覆盖链上每一步，且互不重叠 | 有步骤不属于任何阶段 → 报错 |
| 每个阶段是链上的**连续**区间 | 区间标注会骗人 → 报错 |
| 声明顺序与链序一致 | 总览图会倒着画 → 报错 |

## 已由框架推导的派生值

::: v-pre
| 派生值 | 由什么推导 |
| :--- | :--- |
| 步骤序号（目录名 `steps/NN-id/`、`{{num:id}}`） | 链序 |
| 散文中的步骤引用 `{{step:id}}` | 链序 |
| 步骤的下一跳 `next` | 链序 |
| 阶段边界与区间标注（`(04-06)`） | 阶段意图 + 链序 |
| 流程总览 `flowOverview` | 阶段意图 + 链序 |
| 初始化步骤 `initStepId` | 链起点（入度为 0） |
:::

以上推导共用同一个底座：走一遍链，返回有序 id 列表（下标即序号）。集中一处的原因是——每个派生函数各写一遍遍历，就会各漂移一遍。

三者都遵循同一条规则：**显式声明优先，缺省则由框架推导**。`flowOverview` 覆盖的是**布局**（布局属于表达，框架不垄断），但一旦手写，其中的区间标注就不再有人校验——手写即意味着自己承担漂移风险。

```ts
// 推荐：只声明前驱，顺序与编号由构建期推导
step('scan', '广域扫描')
  .dependsOn('partition')     // ← 只声明这一个方向
  .parallel(/* 步内并行在这里表达 */);

// 引用其他步骤时用标识符，不要手写序号
.target('收敛出可被 {{step:scan}} 消费的 requirement-web.json')
```

::: v-pre
占位符：`{{step:id}}` 渲染为 `Step 04`，`{{num:id}}` 渲染为 `04`（与步骤文件名对齐）。两者都在构建期解析，未解析会直接报错。
:::

## 承诺什么（文本质量）

维护性机制的全部意义就是质量——产物读不通顺，框架就是失败的。以下承诺有机器门兜底，不是姿态：

1. **框架注入不污染产物文本**——渲染器加进产物的每一句（示例题注、附录来源注、章节模板）单独成立、不与作者数据叠加；行级标注由数据侧唯一承载。
2. **产物文本可过散文质量门**——`scanMetaDiscourse`（元话语黑名单）与 `scanMarkerDuplication`（标记叠加）由框架提供、消费仓组装期执行；框架自身产物先过这两道门（回归锁定）。
3. **声明即呈现**——凡作者声明过的输入（含 `.map()` 的 over），产物「文件引用」表必完整呈现（派生补行，零双写）。

## 不承诺什么

承诺的另一面必须同样白纸黑字。以下三条，skillnomad **不承诺**：

1. **理解你 skill 的业务语义**——框架只认结构（步骤、依赖、读写、检查点），不认"扫描""评估"这些词的含义，也不会替你判断内容对不对。
2. **兼容未被声明的行为**——没写进契约的偶然行为（产物里恰好长这样的某个细节），变更时不视为破坏性变更。想依赖它，先把它声明出来。
3. **校验你显式覆盖的派生值**——`next`、区间、总览一旦手写，框架不再验证其与链序的一致性；覆盖即自己承担漂移风险。

::: warning 诚实边界
这三条不是缺陷清单，是边界声明——知道框架管到哪、不管到哪，比"什么都能做"的承诺可信。为什么划在这里 → [设计裁定与不走的路](decisions)。
:::

## 下一步

- 这些承诺背后的取舍与重新考虑的触发条件 → [设计裁定与不走的路](decisions)
- 边界在真实管线里怎么守 → [案例交代](case-study)
- 每个承诺的签名与字段 → [API 导览](../api/types)

